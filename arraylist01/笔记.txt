ArrayList集合元素操作特点
    1.ArrayList存储重复元素，取出的时候直接根据索引取
    2.查询相对较快
        因为ArrayList底层使用数组，数组中的元素可以根据索引直接取出
    3.增删相对较慢
        （添加ml到0索引位置，会引发索引0后面的元素逐个后移）【从尾部元素逐个后移】
        （删除索引0位置的元素 相当于后一索引覆盖前一索引的元素 尾部索引置空）【删除元素导致该元素后面的元素逐个前移】